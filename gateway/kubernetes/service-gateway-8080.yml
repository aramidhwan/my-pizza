apiVersion: v1
kind: Service
metadata:
  name: dev-gateway
  namespace: dev-app
  labels:
    app: gateway
spec:
  ports:
    - port: 8080
      targetPort: 8080
  selector:
    app: gateway
#  type: ClusterIP
#  type: LoadBalancer
