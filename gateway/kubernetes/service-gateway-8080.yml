apiVersion: v1
kind: Service
metadata:
  name: dev-gateway
  namespace: dev-app
  labels:
    app: gateway
spec:
  ports:
    - port: 80
      targetPort: 80
  selector:
    app: gateway
#  type: ClusterIP
#  type: LoadBalancer
