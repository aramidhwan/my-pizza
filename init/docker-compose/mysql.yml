version: '3.8'

services:
  mysql:
    image: mysql
    container_name: mysql
    ports:
      - "3306:3306"
    networks:
      - my-pizza
    environment:
      MYSQL_ROOT_PASSWORD: shin22
      TZ: "Asia/Seoul"

networks:
  my-pizza:
    external: true