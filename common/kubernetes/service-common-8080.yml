apiVersion: v1
kind: Service
metadata:
  name: dev-common
  namespace: dev-app
  labels:
    app: common
spec:
  ports:
    - port: 8080
      targetPort: 8080
  selector:
    app: common
#  type: ClusterIP
#  type: LoadBalancer
