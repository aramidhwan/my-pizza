apiVersion: v1
kind: Service
metadata:
  name: dev-store
  namespace: dev-app
  labels:
    app: store
spec:
  ports:
    - port: 443
      targetPort: 443
  selector:
    app: store
#  type: ClusterIP
#  type: LoadBalancer
