apiVersion: v1
kind: Service
metadata:
  name: dev-order
  namespace: dev-app
  labels:
    app: order
spec:
  ports:
    - port: 8080
      targetPort: 8080
  selector:
    app: order
#  type: ClusterIP
#  type: LoadBalancer
