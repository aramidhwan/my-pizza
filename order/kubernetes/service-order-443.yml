apiVersion: v1
kind: Service
metadata:
  name: dev-order
  namespace: dev-app
  labels:
    app: order
spec:
  ports:
    - port: 443
      targetPort: 443
  selector:
    app: order
#  type: ClusterIP
#  type: LoadBalancer
