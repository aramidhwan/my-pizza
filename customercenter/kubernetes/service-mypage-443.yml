apiVersion: v1
kind: Service
metadata:
  name: dev-mypage
  namespace: dev-app
  labels:
    app: mypage
spec:
  ports:
    - port: 443
      targetPort: 443
  selector:
    app: mypage
#  type: ClusterIP
#  type: LoadBalancer
