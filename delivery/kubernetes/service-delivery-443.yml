apiVersion: v1
kind: Service
metadata:
  name: dev-delivery
  namespace: dev-app
  labels:
    app: delivery
spec:
  ports:
    - port: 443
      targetPort: 443
  selector:
    app: delivery
#  type: ClusterIP
#  type: LoadBalancer
