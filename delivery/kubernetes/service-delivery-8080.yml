apiVersion: v1
kind: Service
metadata:
  name: dev-delivery
  namespace: dev-app
  labels:
    app: delivery
spec:
  ports:
    - port: 8080
      targetPort: 8080
  selector:
    app: delivery
#  type: ClusterIP
#  type: LoadBalancer
